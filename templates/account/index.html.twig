{% extends 'base.html.twig'%}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <div id="banniere">
        <h2 class="titre_secondaire">MON COMPTE</h2>
    </div>
    <section class="ecurie-home">
        <div class="infos-container">
            <a href="{{ path('app_profile_index') }}" role="button">Mes Informations</a>
        </div>
        {% if is_granted('ROLE_ECURIE') %}
            <div class="infos-container">
                {% if app.user.customer.contractFileName is not empty %}
                    <a href="{{ path('app_account_home_download') }}" role="button">Mon Contrat</a>
                    {% else %}
                        <p>Mon contrat</p>
                {% endif %}
            </div>
        {% endif %}
        <div class="infos-container">
            <a href="{{ path('app_account_home_reservationList') }}" role="button">Mes Réservations</a>
        </div>
    </section>
{% endblock %}