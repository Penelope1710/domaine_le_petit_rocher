{% extends 'base.html.twig' %}

{% block title %}Dates indisponibles{% endblock %}

{% block body %}
<div id="banniere">
    <h2 class="titre_secondaire">LES RESERVATIONS DU GITE</h2>
</div>

        <h3 class="titre_tertiaire">Saisir une plage d'indisponibilité</h3>
    <div class="info-display-form unavailabilities-dates">
            {{ form_start(form) }}
                {{ form_row(form.startDate) }}
                {{ form_row(form.endDate) }}
                <div class="button-gpe">
                    <a href="{{ path('app_admin_unavailabilities_list') }}" role="button" class="btn">Annuler</a>
                    <button type="submit">Valider</button>
                </div>
            {{ form_end(form) }}
    </div>

{% endblock %}