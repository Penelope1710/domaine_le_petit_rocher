<nav>
    <div class="nav-accueil">
    <ul>
        <li><a href="{{ path('main_home') }}" title="">Accueil</a></li>
        <li class="dropdown"><a href="#">L'Ecurie</a>
            <ul class="submenu">
                <li><a href="{{ path('ecurie_presentation') }}">Présentation</a></li>
                {% if not app.user %}
                <li><a href="{{ path('app_login') }}">Se connecter</a></li>
                <li><a href="{{ path('app_register_ecurie') }}">S'inscrire</a></li>
                {% endif %}
            </ul>
        </li>
         <li class="dropdown"><a href="#">Le Gîte</a>
            <ul class="submenu">
                <li><a href="{{ path('gite_presentation') }}">Présentation</a></li>
                <li><a href="{{ path('gite_reservation') }}">Réserver</a></li>
{#                <li><a href="{{ path('reservation') }}">Mes réservations</a></li>#}
                {% if not app.user %}
                    <li><a href="{{ path('app_login') }}">Se connecter</a></li>
                    <li><a href="{{ path('app_register_gite') }}">S'inscrire</a></li>
                {% endif %}
            </ul>
         </li>
        {% if is_granted('ROLE_ADMIN') %}
            <li class="dropdown"><a href="#">Administration</a>
                <ul class="submenu">
                    <li><a href="{{ path('admin_user_list') }}">Utilisateurs</a></li>
                    {#                <li><a href="{{ path('reservation') }}">Evènements</a></li>#}
                </ul>
            </li>
        {% endif %}
        <li><a href="{{ path('contact_index') }}" title="">Contact</a></li>
        {% if app.user %}
            {% if is_granted('ROLE_ECURIE') %}
        <li><a href="{{ path('ecurie_home') }}" title="Compte">Mon Compte</a></li>
            {% endif %}
        <li><a href="{{ path('app_logout') }}" title="Déconnexion">Se déconnecter</a></li>
        {% endif %}
    </ul>
    </div>
</nav>



