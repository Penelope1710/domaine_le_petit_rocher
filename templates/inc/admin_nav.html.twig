<nav>

    <div class="nav-accueil">
        <ul>
            <li class="dropdown"><a href="{{ path('main_home') }}" class="nav_link" tabindex="0" aria-haspopup="true">Accueil</a>
                <ul class="submenu" role="menu">
                    <li><a href="{{ path('app_ecurie_home_index') }}">l'écurie</a></li>
                    <li><a href="{{ path('app_gite_home_index') }}">Le gîte</a></li>
                </ul>
            </li>

            <li class="dropdown"><a href="{{ path('app_admin_user_home') }}" class="nav_link" title="administration">Administration</a></li>

            {% if app.user %}
                <li><a href="{{ path('app_logout') }}" class="nav_link" title="Déconnexion">Se déconnecter</a></li>
            {% endif %}
        </ul>
    </div>
</nav>

    <button aria-label="toggle curtain navigation" class="nav-toggler">
        <span class="line l1"></span>
        <span class="line l2"></span>
        <span class="line l3"></span>
        <span class="line l4"></span>
    </button>

    {% block javascripts %}
        <script>
            const hamburgerButton = document.querySelector(".nav-toggler")
            const navigation = document.querySelector(".nav-accueil")

            hamburgerButton.addEventListener("click", toggleNav)

            function toggleNav(){
                hamburgerButton.classList.toggle("active")
                navigation.classList.toggle("active")
            }
        </script>
    {% endblock %}

